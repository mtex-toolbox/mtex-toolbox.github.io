---
title: search
layout: none
search: exclude
---

[
	{% for page in site.pages %}
		{% unless page.search == "exclude" %}
			{
				"title"			: "{{ page.title | strip | escape }}",
				"url"			: "{{ page.url | remove: '/' }}",
				"path"			: "{{ page.url | remove_first: '/' | split: '/' | join: ' > ' }}",
				"date"			: "{{ page.last_updated | replace_first: '-', '. ' | replace_last: '-', ' ' | capitalize }}",
				"keywords"		: "{{ page.keywords }}",
				"content"		: "{{ page.content | strip_html | strip_newlines | replace: '\', '\\\\' | replace: '"', '\\"' | replace: '	', '    ' | replace: '^t', ' ' | strip | escape | truncate: 150 }}"
			}
			{% unless forloop.last and site.posts.size < 1 %},{% endunless %}
		{% endunless %}
	{% endfor %}
	
	{% for post in site.posts %}
		{% unless post.published == false %}
			{
				"title"			: "{{ post.title | strip | escape }}",
				"url"			: "{{ post.url | remove: '/' }}",
				"date"			: "{{ post.date | replace_first: '-', '. ' | capitalize }}",
				"tags"			: "{{ post.tags }}",
				"categories"	: "{{ post.categories }}"
			}
			{% unless forloop.last %},{% endunless %}
		{% endunless %}
	{% endfor %}
]
