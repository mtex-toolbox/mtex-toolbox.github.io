---
title: search
layout: none
search: exclude
---

[
	{% for page in site.pages %}
		{% unless page.search == "exclude" %}
			{
				"title"			: "{{ page.title | strip | escape }}",
				"url"			: "{{ page.url | remove: '/' }}",
				"path"			: "{{ page.url | remove: '.html' | replace: '/', '.' | split: '.' | join: ' > ' | replace_first: ' > ' }}",
				"date"			: "{{ page.last_updated | replace_first: '-', '. ' | replace_first: '-', ' ' }}",
				"keywords"		: "{{ page.keywords }}",
				"content"		: "{{ page.summary | strip | escape | truncate: 150 }}"
			}
			{% unless forloop.last and site.posts.size < 1 %},{% endunless %}
		{% endunless %}
	{% endfor %}
	
	{% for post in site.posts %}
		{% unless post.published == false %}
			{
				"title"			: "{{ post.title | strip | escape }}",
				"url"			: "{{ post.url | remove: '/' }}",
				"date"			: "{{ post.date | replace_first: '-', '. ' | replace_first: '-', ' ' }}",
				"tags"			: "{{ post.tags }}",
				"categories"	: "{{ post.categories }}"
			}
			{% unless forloop.last %},{% endunless %}
		{% endunless %}
	{% endfor %}
]
